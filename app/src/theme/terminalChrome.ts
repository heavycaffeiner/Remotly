// The terminal's own surface colors.
//
// The grid is not a Material surface: it keeps one dark background in either
// theme so a session does not change contrast when the system scheme flips.
// These are the letterbox and overlay colors around the native renderer.

export const TERMINAL_BACKGROUND = '#0b111e';
export const TERMINAL_FOREGROUND = '#e2ebf3';
